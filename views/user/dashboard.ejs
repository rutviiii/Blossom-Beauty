

<div class="container my-5">
<h1 class="page-title">Welcome, <%= user.name %> 👋</h1>
<h3>This is your dashboard.</h3>

  <div class="success-card">
    <div class="success-icon">🌸</div>
    <h1>Explore Our Products!</h1>
    <p>Thank you for shopping with <strong>Blossom Beauty</strong>. </p><br>
    <a href="/user/products" class="btn-shop">Browse Products</a>
  </div>

<style>
  body{

     background: linear-gradient(135deg, #f9f9f9, #ffe6f0);
      justify-content: center;
      align-items: center;
      font-family: 'Segoe UI', sans-serif;
    }

    .success-card {
      background: #fff;
      border-radius: 20px;
      box-shadow: 0 8px 20px rgba(0,0,0,0.1);
      text-align: center;
      padding: 50px;
      max-width: 600px;
      width: 100%;
      animation: fadeInUp 1s ease;
    }
    .success-icon {
      font-size: 80px;
      color: #28a745;
      margin-bottom: 20px;
      animation: bounce 1.5s infinite;
    }
    h1 {
      font-weight: bold;
      color: #333;
    }
    p {
      font-size: 18px;
      color: #666;
    }
    .btn-shop {
      margin-top: 25px;
      padding: 12px 30px;
      font-size: 18px;
      border-radius: 30px;
      background: #ff4da6;
      color: #fff;
      border: none;
      transition: 0.3s;
    }
    .btn-shop:hover {
      background: #e60073;
      transform: scale(1.05);
    }

    /* Animations */
    @keyframes fadeInUp {
      from {opacity: 0; transform: translateY(30px);}
      to {opacity: 1; transform: translateY(0);}
    }
    @keyframes bounce {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-8px); }
    }
  
</style>

</div>
